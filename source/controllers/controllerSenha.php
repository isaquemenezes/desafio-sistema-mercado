<?php
    // $validate=new Classes\ClassValidate();

    // // Valida todos os campos de formulário , $_POST,  
    // $validate->validateFields($_POST);             

    // // validação do email,
    // $validate->validateEmail($email);    

    // // verificação se o email está ou não no banco de dados,
    // $validate->validateIssetEmail($email, "senha");  

    // //verificação de senhas dígitas são iguais 
    // $validate->validateConfirmacaoSenha($senha,$senhaConf); 
    
    // //--recuperacao de senha,
    // $validate->validateEmailRecuperaSenha($email);  
    
    // //Verificação Final de Senha 
    // echo $validate->validateFinalSenha($arrayVar);

    echo "hi controller senha";